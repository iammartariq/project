import React from "react";

interface SignUpFormProps {
   setIsSignupModelOpen: (open: boolean) => void;
}

const SignUpForm: React.FC<SignUpFormProps> = ({ setIsSignupModelOpen }) => {
   return (
      <div>
         {/* SignUp Form Component */}
      </div>
   );
};

export default SignUpForm;